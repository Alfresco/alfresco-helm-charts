---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "alfresco-connector-cic.live-ingester.fullname" . }}
  labels:
    {{- include "alfresco-connector-cic.live-ingester.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.liveIngester.replicaCount | default 1 }}
  selector:
    matchLabels:
      {{- include "alfresco-connector-cic.live-ingester.selectorLabels" . | nindent 6 }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      {{- toYaml .Values.liveIngester.strategy.rollingUpdate | nindent 6 }}
  template:
    metadata:
      {{- with $.Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "alfresco-connector-cic.live-ingester.labels" . | nindent 8 }}
    spec:
      {{- include "alfresco-common.imagePullSecrets" . | indent 6 }}
      serviceAccountName: {{ include "alfresco-connector-cic.serviceAccountName" . }}
      {{- include "alfresco-common.component-pod-security-context" .Values | indent 4 }}
      containers:
        - name: {{ include "alfresco-connector-cic.live-ingester.name" . }}
          image: {{ printf "%s:%s" .Values.liveIngester.image.repository .Values.liveIngester.image.tag | quote }}
          imagePullPolicy: {{ .Values.liveIngester.image.pullPolicy }}
          {{- include "alfresco-common.component-security-context" .Values | indent 8 }}
          env:
            {{- include "alfresco-connector-cic.activemq.cm.env" . | indent 12 }}
            {{- include "alfresco-connector-cic.activemq.secret.env" . | indent 12 }}
            {{- include "alfresco-connector-cic.ats.cm.env" . | indent 12 }}
            {{- include "alfresco-connector-cic.repository.cm.env" . | indent 12 }}
            {{- include "alfresco-connector-cic.repository.secret.env" . | indent 12 }}
            {{- include "alfresco-connector-cic.live-ingester.cm.env" . | indent 12 }}
            {{- include "alfresco-connector-cic.secret.env" . | indent 12 }}
            {{- range $key, $val := $.Values.liveIngester.environment }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
          ports:
            - containerPort: {{ .Values.liveIngester.image.internalPort }}
          resources:
            {{- toYaml .Values.liveIngester.resources | nindent 12 }}
          livenessProbe:
            {{- toYaml .Values.liveIngester.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.liveIngester.readinessProbe | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.liveIngester.affinity }}
      affinity:
        {{- tpl (toYaml .) $ | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
