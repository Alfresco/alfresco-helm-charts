---
{{- if .Values.nucleusSync.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "alfresco-connector-cic.nucleus-sync.fullname" . }}
  labels:
    {{- include "alfresco-connector-cic.nucleus-sync.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.nucleusSync.replicaCount | default 1 }}
  selector:
    matchLabels:
      {{- include "alfresco-connector-cic.nucleus-sync.selectorLabels" . | nindent 6 }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      {{- toYaml .Values.nucleusSync.strategy.rollingUpdate | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap-connector-cic.yaml") $ | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/secret-connector-cic.yaml") $ | sha256sum }}
        checksum/secret-broker: {{ include (print $.Template.BasePath "/secret-mq.yaml") $ | sha256sum }}
        {{- with $.Values.podAnnotations }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "alfresco-connector-cic.nucleus-sync.labels" . | nindent 8 }}
    spec:
      {{- include "alfresco-common.imagePullSecrets" . | indent 6 }}
      serviceAccountName: {{ include "alfresco-connector-cic.serviceAccountName" . }}
      {{- include "alfresco-common.component-pod-security-context" .Values | indent 4 }}
      containers:
        - name: {{ include "alfresco-connector-cic.nucleus-sync.name" . }}
          image: {{ printf "%s:%s" .Values.nucleusSync.image.repository .Values.nucleusSync.image.tag | quote }}
          imagePullPolicy: {{ .Values.nucleusSync.image.pullPolicy }}
          {{- include "alfresco-common.component-security-context" .Values | indent 8 }}
          env:
            {{- include "alfresco-connector-cic.nucleus-sync.repository.cm.env" . | indent 12 }}
            {{- include "alfresco-connector-cic.nucleus-sync.repository.secret.env" . | indent 12 }}
            {{- include "alfresco-connector-cic.nucleus-sync.cm.env" . | indent 12 }}
            {{- include "alfresco-connector-cic.nucleus-sync.secret.env" . | indent 12 }}
            {{- range $key, $val := $.Values.nucleusSync.environment }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
          ports:
            - containerPort: {{ .Values.nucleusSync.image.internalPort }}
          resources:
            {{- toYaml .Values.nucleusSync.resources | nindent 12 }}
          livenessProbe:
            {{- toYaml .Values.nucleusSync.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.nucleusSync.readinessProbe | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nucleusSync.affinity }}
      affinity:
        {{- tpl (toYaml .) $ | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
