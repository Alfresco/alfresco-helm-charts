{{- if gt .Values.replicaCount 1 }}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "alfresco-share.service-hz.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: hazelcast
      port: 5701
  selector:
    app: {{ template "alfresco-share.service-hz.name" . }}
{{- end -}}
