---
suite: test repository service account
templates:
  - serviceaccount.yaml
tests:
  - it: should render a service account by default
    asserts:
      - hasDocuments:
          count: 1

  - it: should not render a service account when disabled
    set:
      serviceAccount:
        create: false
    asserts:
      - hasDocuments:
          count: 0
