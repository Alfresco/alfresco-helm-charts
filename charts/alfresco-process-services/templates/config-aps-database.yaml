{{- if not .Values.database.existingConfigMap.name }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: >-
    {{ template "alfresco-process-services.database-aps" . }}
  labels:
    app: {{ template "alfresco-process-services.fullname" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  {{- with .Values.database }}
  DATABASE_URL: {{ hasPrefix "jdbc:" .url.aps | ternary .url.aps (print "jdbc:" .url.aps) }}
  {{- end }}
{{- end }}
