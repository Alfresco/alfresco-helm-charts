{{- $componentScope := (dict "Values" (dict "nameOverride" "llibreoffice" ) "Chart" .Chart "Release" .Release) }}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "alfresco-transform-service.fullname" $componentScope }}
  labels:
    {{- include "alfresco-transform-service.labels" $componentScope | nindent 4 }}
spec:
  type: {{ .Values.libreoffice.service.type }}
  ports:
    - port: {{ .Values.libreoffice.service.externalPort }}
      targetPort: {{ .Values.libreoffice.image.internalPort }}
      name: {{ .Values.libreoffice.service.name }}
  selector:
    {{- include "alfresco-transform-service.selectorLabels" $componentScope | nindent 4 }}
