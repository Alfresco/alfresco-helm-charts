{{- $componentScope := (dict "Values" (dict "nameOverride" "pdfrenderer" ) "Chart" .Chart "Release" .Release) }}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "alfresco-transform-service.fullname" $componentScope }}
  labels:
    {{- include "alfresco-transform-service.labels" $componentScope | nindent 4 }}
spec:
  type: {{ .Values.pdfrenderer.service.type }}
  ports:
    - port: {{ .Values.pdfrenderer.service.externalPort }}
      targetPort: {{ .Values.pdfrenderer.image.internalPort }}
      name: {{ .Values.pdfrenderer.service.name }}
  selector:
    {{- include "alfresco-transform-service.selectorLabels" $componentScope | nindent 4 }}
