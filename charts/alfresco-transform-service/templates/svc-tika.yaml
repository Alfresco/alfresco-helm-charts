{{- $componentScope := (dict "Values" (dict "nameOverride" "tika" ) "Chart" .Chart "Release" .Release) }}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "alfresco-transform-service.fullname" $componentScope }}
  labels:
    {{- include "alfresco-transform-service.labels" $componentScope | nindent 4 }}
spec:
  type: {{ .Values.tika.service.type }}
  ports:
    - port: {{ .Values.tika.service.externalPort }}
      targetPort: {{ .Values.tika.image.internalPort }}
      name: {{ .Values.tika.service.name }}
  selector:
    {{- include "alfresco-transform-service.selectorLabels" $componentScope | nindent 4 }}
